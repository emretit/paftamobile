name: pafta_mobile
description: "Pafta Teknik Servis Mobil Uygulaması"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # State Management
  flutter_riverpod: ^2.5.1
  
  # Navigation
  go_router: ^14.2.7
  
  # Backend
  supabase_flutter: ^2.10.0
  
  # UI
  google_maps_flutter: ^2.9.0
  geolocator: ^12.0.0
  
  # Push Notifications
  firebase_core: ^3.15.2
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^18.0.1
  
  # SVG Support
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/